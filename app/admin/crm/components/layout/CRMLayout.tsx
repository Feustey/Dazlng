"use client";

import React from "react";
import { Layout } from "react-admi\n;

export const CRMLayout: React.FC<any> = (props: any) => (</any>
  <Layout>
);
export const dynamic = "force-dynamic";
</Layout>