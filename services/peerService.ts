import { supabase } from "../lib/supabase";

// Classe vide pour conserver la structure du code
// sans les fonctionnalités liées aux réseaux, canaux et nœuds
export class PeerService {
  // Cette classe a été vidée car Dazbox ne s'occupe plus de fonctionnalités réseau
  // Elle est conservée pour maintenir la structure mais pourrait être supprimée ultérieurement
}

export const peerService = new PeerService();
